routes:
  /signup/: members/signup
  /signin/: members/signin
  /account/: members/account

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: tag:-[featured-top]

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/